# Changesets

This directory contains changeset files generated by `pnpm changeset`.

To add a changeset:

```sh
pnpm changeset
```

Select the packages that changed and the semver bump type, then write a summary of the changes.

Changesets in this directory will be consumed during release and turned into changelog entries. Delete them after release.
